/**
 * Global layout/zoom configuration.
 * Controls the overall scale of the entire application UI.
 */
export const LAYOUT_CONFIG = {
    /**
     * Scale factor for the entire app — analogous to `Ctrl+Minus` in the browser.
     * Default: 0.8 ≈ 80%
     */
    zoomScale: 0.8,
} as const